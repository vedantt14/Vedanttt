// 23. Calculate area of rectangle
#include <stdio.h>
int main() {
    float l, w;
    printf("Enter length and width: ");
    scanf("%f%f", &l, &w);
    printf("Area = %.2f", l * w);
    return 0;
}
