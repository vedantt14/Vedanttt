// Program 10: Add two float numbers from user
#include <stdio.h>
int main() {
    float a, b;
    printf("Enter two float numbers: ");
    scanf("%f %f", &a, &b);
    printf("Sum = %.2f", a + b);
    return 0;
}
